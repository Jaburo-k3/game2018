using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Debug_pro : MonoBehaviour {
    public GameObject Deb_obj;
    public Vector3 Deb_vec3;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //Debug.Log("position = " + Deb_obj.transform.position);
        //Debug.Log("local = " + Deb_obj.transform.localPosition);
    }
}
